object question1 {
  var k,i,j : Int = 2
  var m,n : Int = 5
  var f : Double = 12.0f
  var g : Double = 4.0f
  var c : Char = 'X'

  def main(args: Array[String]): Unit = {
    println("a) k + 12 * m = " + (k + (12 * m)))
    println("b) m / j = " + (m / j))
    println("c) n % j = " + (n % j))
    println("d) m / j * j = " + (m / j * j))
    println("e) f + 10 * 5 + g = " + (f + 10 * 5 + g ))
    i += 1
    println("f) ++i * n = " + (i * n ))   // ++ operator is not supported in scala so we have to manually do it
  }
}